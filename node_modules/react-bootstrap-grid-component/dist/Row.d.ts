import * as React from 'react';
import { Viewport } from './config';
import './sizingbreakpoints.scss';
declare type VerticalAlignment = 'center' | 'start' | 'end';
declare type VerticalAlignmentViewport = {
    [key in Viewport]?: VerticalAlignment;
};
declare type HorizontalAlignment = 'start' | 'center' | 'between' | 'end';
declare type HorizontalAlignmentViewport = {
    [key in Viewport]?: HorizontalAlignment;
};
interface IRowProps {
    /**
     * https://getbootstrap.com/docs/4.0/layout/grid/#vertical-alignment
     */
    verticalAlignment?: VerticalAlignment | VerticalAlignmentViewport;
    /**
     * https://getbootstrap.com/docs/4.0/layout/grid/#horizontal-alignment
     */
    horizontalAlignment?: HorizontalAlignment | HorizontalAlignmentViewport;
    /**
     * https://getbootstrap.com/docs/4.0/layout/grid/#no-gutters
     */
    noGutters?: boolean;
    /**
     * Rows must contain only columns to prevent negative margin issues
     */
    children: React.ReactNode;
    className?: string;
}
export declare const Row: ({ verticalAlignment, horizontalAlignment, noGutters, children, className }: IRowProps) => JSX.Element;
export {};
